#pragma once

namespace ti::backend {
class ResourceImageSampler {
public:
    struct Description {
    };

protected:
    ResourceImageSampler() = default;
    virtual ~ResourceImageSampler() = default;
};
}
